<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>$(OpenCSTargetFramework)</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\CSmath\CSmath.csproj" />
    <ProjectReference Include="..\CSTriangulation\CSTriangulation.csproj" />
  </ItemGroup>

  <ItemGroup>
    <!-- Encoding.GetEncoding(1251) для ScadTextParser даёт CodePagesEncodingProvider, он входит
         в сам .NET (отдельный пакет System.Text.Encoding.CodePages не нужен), но регистрировать
         провайдер всё равно надо явно. -->
    <PackageReference Include="ExcelDataReader" Version="3.9.0" />
  </ItemGroup>

</Project>
