<Window x:Class="OpenCS.Views.Dialogs.CircleDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Добавление окружности"
        Width="300" SizeToContent="Height"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize" ShowInTaskbar="False">
   <Grid Margin="16">
      <Grid.RowDefinitions>
         <RowDefinition Height="Auto"/>
         <RowDefinition Height="Auto"/>
         <RowDefinition Height="Auto"/>
         <RowDefinition Height="Auto"/>
      </Grid.RowDefinitions>
      <Grid.ColumnDefinitions>
         <ColumnDefinition Width="100"/>
         <ColumnDefinition Width="*"/>
      </Grid.ColumnDefinitions>

      <TextBlock Grid.Row="0" Grid.Column="0" Text="X, м:" VerticalAlignment="Center" Margin="0,0,8,8"/>
      <TextBox Grid.Row="0" Grid.Column="1" x:Name="XBox" Text="0" Margin="0,0,0,8"/>

      <TextBlock Grid.Row="1" Grid.Column="0" Text="Y, м:" VerticalAlignment="Center" Margin="0,0,8,8"/>
      <TextBox Grid.Row="1" Grid.Column="1" x:Name="YBox" Text="0" Margin="0,0,0,8"/>

      <TextBlock Grid.Row="2" Grid.Column="0" Text="Радиус, м:" VerticalAlignment="Center" Margin="0,0,8,8"/>
      <TextBox Grid.Row="2" Grid.Column="1" x:Name="RadiusBox" Text="0.01" Margin="0,0,0,8"/>

      <StackPanel Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
         <Button Content="OK" IsDefault="True" Click="Ok_Click" Width="90" Margin="0,0,8,0"/>
         <Button Content="Отмена" IsCancel="True" Width="90"/>
      </StackPanel>
   </Grid>
</Window>
