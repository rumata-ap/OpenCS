<UserControl x:Class="OpenCS.Views.ShellSimplBatchResultView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
   <Grid Margin="12">
      <Grid.RowDefinitions>
         <RowDefinition Height="Auto"/>
         <RowDefinition Height="*"/>
      </Grid.RowDefinitions>

      <!-- Шапка: вердикт -->
      <Border Grid.Row="0" Padding="8,6" Margin="0,0,0,8" CornerRadius="4"
              Background="{Binding StatusBrush}">
         <TextBlock Text="{Binding Summary}" FontWeight="Bold"
                    HorizontalAlignment="Center"/>
      </Border>

      <!-- Таблица строк -->
      <DataGrid Grid.Row="1" ItemsSource="{Binding Rows}" AutoGenerateColumns="False"
                IsReadOnly="True" GridLinesVisibility="Horizontal"
                CanUserSortColumns="True" RowHeaderWidth="0">
         <DataGrid.RowStyle>
            <Style TargetType="DataGridRow">
               <Style.Triggers>
                  <DataTrigger Binding="{Binding Ok}" Value="True">
                     <Setter Property="Background" Value="#1A2D7A3E"/>
                  </DataTrigger>
                  <DataTrigger Binding="{Binding Ok}" Value="False">
                     <Setter Property="Background" Value="#1AC0392B"/>
                  </DataTrigger>
               </Style.Triggers>
            </Style>
         </DataGrid.RowStyle>
         <DataGrid.Columns>
            <DataGridTextColumn Header="{DynamicResource StrainStateBatch_ColNum}" Binding="{Binding Num}" Width="40"/>
            <DataGridTextColumn Header="{DynamicResource StrainStateBatch_ColLabel}" Binding="{Binding Label}" Width="*"/>
            <DataGridTextColumn Header="η_max" Binding="{Binding EtaMaxDisplay}"/>
            <DataGridTextColumn Header="Статус" Binding="{Binding Status}"/>
         </DataGrid.Columns>
      </DataGrid>
   </Grid>
</UserControl>
