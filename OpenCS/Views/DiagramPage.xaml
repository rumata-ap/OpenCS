<UserControl x:Class="OpenCS.Views.DiagramPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:OpenCS.Views"
             mc:Ignorable="d"
             d:DesignHeight="500" d:DesignWidth="700"
             Background="White">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#FF2813EB" CornerRadius="4" Padding="8" Margin="0,0,0,8">
            <TextBlock x:Name="titleText" Foreground="White" FontSize="15" FontWeight="SemiBold"
                       HorizontalAlignment="Center"/>
        </Border>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,8">
            <TextBlock x:Name="infoText" FontSize="13" Foreground="#333"/>
        </StackPanel>

        <TabControl Grid.Row="2" Margin="0,0,0,5">
            <TabItem Header="{DynamicResource Chart}">
                <local:PlotCanvas x:Name="plot" Margin="5" />
            </TabItem>
            <TabItem Header="{DynamicResource Data}">
                <Grid Margin="5">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <DataGrid x:Name="dataGrid" AutoGenerateColumns="True" IsReadOnly="True"
                              HorizontalAlignment="Stretch" GridLinesVisibility="All"/>
                    <Button Grid.Row="1" Content="{DynamicResource ExportCsv}" Click="ExportCsv_Click"
                            Width="140" Height="28" Margin="0,8,0,0" HorizontalAlignment="Right"/>
                </Grid>
            </TabItem>
        </TabControl>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
            <Button x:Name="deleteButton" Content="{DynamicResource Delete}" Click="Delete_Click"
                    Width="90" Height="30" Margin="0,0,8,0"/>
            <Button x:Name="closeButton" Content="{DynamicResource Close}" Click="Close_Click"
                    Width="90" Height="30"/>
        </StackPanel>
    </Grid>
</UserControl>
