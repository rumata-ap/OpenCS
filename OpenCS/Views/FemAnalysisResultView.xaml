<UserControl x:Class="OpenCS.Views.FemAnalysisResultView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:h="clr-namespace:HelixToolkit.Wpf;assembly=HelixToolkit.Wpf"
             xmlns:u="clr-namespace:OpenCS.Utilites"
             Language="ru-RU">
    <UserControl.Resources>
        <u:AnyDoubleConverter x:Key="AnyDoubleConv"/>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
        <ContextMenu x:Key="ResultMemberContextMenu">
            <MenuItem Header="{DynamicResource FemResultMember2D}" Click="MemberShow2DCtx_Click"/>
            <MenuItem Header="{DynamicResource FemResultMemberSection}" Click="MemberSectionCtx_Click"/>
        </ContextMenu>
        <ContextMenu x:Key="ResultNodeContextMenu">
            <MenuItem Header="{DynamicResource FemResultNodeValues}" Click="NodeValuesCtx_Click"/>
        </ContextMenu>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TabControl Grid.Row="0">
            <!-- 3D -->
            <TabItem Header="{DynamicResource FemResultTab3D}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="4">
                        <TextBlock Text="{DynamicResource FemResultDeformScale}" VerticalAlignment="Center"/>
                        <TextBox Width="60" Text="{Binding DeformScale, Converter={StaticResource AnyDoubleConv}, UpdateSourceTrigger=PropertyChanged, Delay=500}" VerticalAlignment="Center" Margin="8,0"/>
                        <Button Style="{DynamicResource IconButton}" Content="↺" ToolTip="{DynamicResource FemResetScale}" Command="{Binding ResetDeformScaleCommand}" Margin="0,0,10,0"/>
                        <Separator Margin="10,0"/>
                        <CheckBox x:Name="showNodesCheck" Content="{DynamicResource FemShowNodes}" VerticalAlignment="Center" IsChecked="True" Click="NodesToggle" Margin="0,0,10,0"/>
                        <Separator Margin="10,0"/>
                        <TextBlock Text="{DynamicResource FemResultForceComponent}" VerticalAlignment="Center"/>
                        <ComboBox Width="70" Margin="6,0" VerticalAlignment="Center"
                                  ItemsSource="{Binding ForceComponents}"
                                  SelectedItem="{Binding SelectedForceComponent, Mode=TwoWay}"/>
                        <TextBlock Text="{DynamicResource FemResultForceScale}" VerticalAlignment="Center" Margin="8,0,0,0"/>
                        <TextBox Width="60" Text="{Binding ForceScale, Converter={StaticResource AnyDoubleConv}, UpdateSourceTrigger=PropertyChanged, Delay=500}" VerticalAlignment="Center" Margin="8,0"/>
                        <Button Style="{DynamicResource IconButton}" Content="↺" ToolTip="{DynamicResource FemResetScale}" Command="{Binding ResetForceScaleCommand}"/>
                    </StackPanel>
                    <h:HelixViewport3D x:Name="viewport" Grid.Row="1" Background="White"
                                       ShowCoordinateSystem="True" ShowViewCube="True"
                                       ZoomAroundMouseDownPoint="True"
                                       PreviewMouseRightButtonDown="Viewport_PreviewMouseRightButtonDown"/>
                    <Border Grid.Row="1" Background="#FFF0F0" BorderBrush="Red" BorderThickness="1" 
                            VerticalAlignment="Top" HorizontalAlignment="Left" Margin="10"
                            Visibility="{Binding HasDiagnostics, Converter={StaticResource BoolToVis}}">
                        <ItemsControl ItemsSource="{Binding Diagnostics}" Margin="10">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding}" Foreground="Red" TextWrapping="Wrap" MaxWidth="600"/>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </Border>
                </Grid>
            </TabItem>

            <!-- Перемещения -->
            <TabItem Header="{DynamicResource FemResultTabDisp}">
                <DataGrid ItemsSource="{Binding Displacements}" AutoGenerateColumns="False"
                          IsReadOnly="True" CanUserAddRows="False">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="{DynamicResource FemColNode}" Binding="{Binding NodeTag}"/>
                        <DataGridTextColumn Header="Ux" Binding="{Binding Ux, StringFormat=G6}"/>
                        <DataGridTextColumn Header="Uy" Binding="{Binding Uy, StringFormat=G6}"/>
                        <DataGridTextColumn Header="Uz" Binding="{Binding Uz, StringFormat=G6}"/>
                        <DataGridTextColumn Header="Rx" Binding="{Binding Rx, StringFormat=G6}"/>
                        <DataGridTextColumn Header="Ry" Binding="{Binding Ry, StringFormat=G6}"/>
                        <DataGridTextColumn Header="Rz" Binding="{Binding Rz, StringFormat=G6}"/>
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>

            <!-- Реакции -->
            <TabItem Header="{DynamicResource FemResultTabReact}">
                <DataGrid ItemsSource="{Binding Reactions}" AutoGenerateColumns="False"
                          IsReadOnly="True" CanUserAddRows="False">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="{DynamicResource FemColNode}" Binding="{Binding NodeTag}"/>
                        <DataGridTextColumn Header="Rx" Binding="{Binding Rx, StringFormat=G6}"/>
                        <DataGridTextColumn Header="Ry" Binding="{Binding Ry, StringFormat=G6}"/>
                        <DataGridTextColumn Header="Rz" Binding="{Binding Rz, StringFormat=G6}"/>
                        <DataGridTextColumn Header="Mx" Binding="{Binding Mx, StringFormat=G6}"/>
                        <DataGridTextColumn Header="My" Binding="{Binding My, StringFormat=G6}"/>
                        <DataGridTextColumn Header="Mz" Binding="{Binding Mz, StringFormat=G6}"/>
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>

            <!-- Усилия -->
            <TabItem Header="{DynamicResource FemResultTabForces}">
                <DataGrid ItemsSource="{Binding ElementForces}" AutoGenerateColumns="False"
                          IsReadOnly="True" CanUserAddRows="False">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="{DynamicResource FemColElement}" Binding="{Binding ElemTag}"/>
                        <DataGridTextColumn Header="N(i)"  Binding="{Binding Ni,  StringFormat=G6}"/>
                        <DataGridTextColumn Header="Qy(i)" Binding="{Binding Qyi, StringFormat=G6}"/>
                        <DataGridTextColumn Header="Qz(i)" Binding="{Binding Qzi, StringFormat=G6}"/>
                        <DataGridTextColumn Header="Mx(i)" Binding="{Binding Mxi, StringFormat=G6}"/>
                        <DataGridTextColumn Header="My(i)" Binding="{Binding Myi, StringFormat=G6}"/>
                        <DataGridTextColumn Header="Mz(i)" Binding="{Binding Mzi, StringFormat=G6}"/>
                        <DataGridTextColumn Header="N(j)"  Binding="{Binding Nj,  StringFormat=G6}"/>
                        <DataGridTextColumn Header="Qy(j)" Binding="{Binding Qyj, StringFormat=G6}"/>
                        <DataGridTextColumn Header="Qz(j)" Binding="{Binding Qzj, StringFormat=G6}"/>
                        <DataGridTextColumn Header="Mx(j)" Binding="{Binding Mxj, StringFormat=G6}"/>
                        <DataGridTextColumn Header="My(j)" Binding="{Binding Myj, StringFormat=G6}"/>
                        <DataGridTextColumn Header="Mz(j)" Binding="{Binding Mzj, StringFormat=G6}"/>
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>
        </TabControl>


    </Grid>
</UserControl>
