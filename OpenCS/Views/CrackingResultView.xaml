<UserControl x:Class="OpenCS.Views.CrackingResultView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
   <Grid Margin="16">
      <StackPanel>
         <Border Padding="12,8" CornerRadius="4" Background="{Binding StatusBrush}" Margin="0,0,0,12">
            <TextBlock Text="{Binding SummaryText}" HorizontalAlignment="Center" FontWeight="Bold" FontSize="14"/>
         </Border>

         <Border Padding="8,6" CornerRadius="4" Background="#F0F4F8" BorderBrush="#D5DDE5" BorderThickness="1">
            <StackPanel>
               <TextBlock Margin="0,2"><Run Text="N, кН: "/><Run Text="{Binding NText, Mode=OneWay}" FontWeight="Bold"/></TextBlock>
               <TextBlock Margin="0,2"><Run Text="Mx,crc, кН·м: "/><Run Text="{Binding MxCrcText, Mode=OneWay}" FontWeight="Bold"/></TextBlock>
               <TextBlock Margin="0,2"><Run Text="My,crc, кН·м: "/><Run Text="{Binding MyCrcText, Mode=OneWay}" FontWeight="Bold"/></TextBlock>
               <TextBlock Margin="0,2"><Run Text="Mcrc, кН·м: "/><Run Text="{Binding McrcText, Mode=OneWay}" FontWeight="Bold" FontSize="15"/></TextBlock>
            </StackPanel>
         </Border>

         <TextBlock Text="{Binding ErrorText}" Foreground="DarkRed" TextWrapping="Wrap" Margin="0,8,0,0"
                    Visibility="{Binding HasError, Converter={StaticResource BoolToVisibility}}"/>
      </StackPanel>
   </Grid>
</UserControl>
