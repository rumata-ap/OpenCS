<Window x:Class="OpenCS.Views.LiraElemRangeDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{DynamicResource LiraElemRangeDlgTitle}"
        Width="380" SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False">
   <Grid Margin="12">
      <Grid.RowDefinitions>
         <RowDefinition Height="Auto"/>
         <RowDefinition Height="Auto"/>
         <RowDefinition Height="Auto"/>
      </Grid.RowDefinitions>
      <TextBlock Grid.Row="0" Text="{DynamicResource LiraElemRangeDlgPrompt}"
                 TextWrapping="Wrap" Margin="0,0,0,8"/>
      <TextBox x:Name="RangeBox" Grid.Row="1" Margin="0,0,0,12"
               Text="{Binding Range, UpdateSourceTrigger=PropertyChanged}"
               ToolTip="{DynamicResource LiraElemRangeDlgHint}"/>
      <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
         <Button Content="{DynamicResource OK}" IsDefault="True"
                 Click="Ok_Click" Padding="20,4" Margin="0,0,8,0"/>
         <Button Content="{DynamicResource Cancel}" IsCancel="True" Padding="16,4"/>
      </StackPanel>
   </Grid>
</Window>
