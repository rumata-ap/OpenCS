<Window x:Class="OpenCS.Views.FemNodeLoadDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{DynamicResource FemNodeLoadDialogTitle}"
        Width="430" SizeToContent="Height" WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False">
    <StackPanel Margin="14">
        <TextBlock x:Name="selectionText" FontWeight="Bold" Margin="0,0,0,8"/>
        <TextBlock Text="{DynamicResource FemNodePropsLoadCase}"/>
        <ComboBox x:Name="loadCaseCombo" DisplayMemberPath="Tag"
                  SelectionChanged="LoadCaseCombo_SelectionChanged" Margin="0,2,0,8"/>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/><RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition/><ColumnDefinition/><ColumnDefinition/>
                <ColumnDefinition/><ColumnDefinition/><ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="{DynamicResource FemNodePropsFx}" Grid.Column="0" Margin="1"/>
            <TextBlock Text="{DynamicResource FemNodePropsFy}" Grid.Column="1" Margin="1"/>
            <TextBlock Text="{DynamicResource FemNodePropsFz}" Grid.Column="2" Margin="1"/>
            <TextBlock Text="{DynamicResource FemNodePropsMx}" Grid.Column="3" Margin="1"/>
            <TextBlock Text="{DynamicResource FemNodePropsMy}" Grid.Column="4" Margin="1"/>
            <TextBlock Text="{DynamicResource FemNodePropsMz}" Grid.Column="5" Margin="1"/>
            <TextBox x:Name="fxBox" Grid.Row="1" Grid.Column="0" Margin="1"/>
            <TextBox x:Name="fyBox" Grid.Row="1" Grid.Column="1" Margin="1"/>
            <TextBox x:Name="fzBox" Grid.Row="1" Grid.Column="2" Margin="1"/>
            <TextBox x:Name="mxBox" Grid.Row="1" Grid.Column="3" Margin="1"/>
            <TextBox x:Name="myBox" Grid.Row="1" Grid.Column="4" Margin="1"/>
            <TextBox x:Name="mzBox" Grid.Row="1" Grid.Column="5" Margin="1"/>
        </Grid>
        <TextBlock Text="{DynamicResource FemNodePropsLoadUnitsHint}" Foreground="Gray"
                   FontSize="11" Margin="1,4,1,8"/>
        <Button Content="{DynamicResource FemNodeLoadDialogApply}" Click="Apply_Click"/>
    </StackPanel>
</Window>
