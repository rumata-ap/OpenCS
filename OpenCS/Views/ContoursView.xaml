<UserControl x:Class="OpenCS.Views.ContoursView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:OpenCS.Views"
             xmlns:vm="clr-namespace:OpenCS"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

   <Grid>
      <ListBox x:Name="contoursList" ItemsSource="{Binding ContoursLive}" ItemTemplate="{StaticResource ContourBig}"
               ScrollViewer.HorizontalScrollBarVisibility="Disabled">
         <ListBox.ItemsPanel>
            <ItemsPanelTemplate>
               <WrapPanel />
            </ItemsPanelTemplate>
         </ListBox.ItemsPanel>
      </ListBox>
   </Grid>
</UserControl>
