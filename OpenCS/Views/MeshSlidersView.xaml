<UserControl x:Class="OpenCS.Views.MeshSlidersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="200">
   <Grid>
      <Grid.RowDefinitions>
         <RowDefinition/>
         <RowDefinition/>
         <RowDefinition/>
         <RowDefinition Height="Auto"/>
      </Grid.RowDefinitions>
      <Grid.ColumnDefinitions>
         <ColumnDefinition Width="Auto"/>
         <ColumnDefinition/>
         <ColumnDefinition Width="Auto"/>
      </Grid.ColumnDefinitions>

      <Slider Grid.Column="1" Margin="5"
              Minimum="1" Maximum="60"
              TickPlacement="BottomRight"
              TickFrequency="1" ToolTip="{DynamicResource ToolTip_nX}"
              Value="{Binding Dx}"/>

      <Slider Grid.Row="1" Margin="5"
              Orientation="Vertical"
              Minimum="1" Maximum="60"
              TickPlacement="BottomRight"
              TickFrequency="2" ToolTip="{DynamicResource ToolTip_nY}"
              Value="{Binding Dy}"/>

      <Slider Grid.Row="1" Grid.Column="2"
              Orientation="Vertical" Margin="5"
              Minimum="0.05" Maximum="1"
              TickPlacement="TopLeft"
              TickFrequency="0.05"
              Value="{Binding Atr}"/>

      <Slider Grid.Row="2" Grid.Column="1" Margin="5"
              Minimum="10" Maximum="30"
              TickPlacement="TopLeft"
              TickFrequency="0.5"
              Value="{Binding Antr}"/>
   </Grid>
</UserControl>