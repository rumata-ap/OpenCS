<Window x:Class="OpenCS.Views.CsvSettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{DynamicResource CsvExportTitle}" Width="360" Height="260"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <GroupBox Grid.Row="0" Header="{DynamicResource ColumnDelimiter}" Margin="0,0,0,10">
            <StackPanel>
                <RadioButton x:Name="CsvSemicolon" Content="{DynamicResource Semicolon}" Margin="0,3" IsChecked="True"/>
                <RadioButton x:Name="CsvComma" Content="{DynamicResource Comma}" Margin="0,3"/>
            </StackPanel>
        </GroupBox>

        <GroupBox Grid.Row="1" Header="{DynamicResource Encoding}">
            <StackPanel>
                <RadioButton x:Name="CsvUtf8" Content="UTF-8" Margin="0,3" IsChecked="True"/>
                <RadioButton x:Name="CsvWin1251" Content="Windows-1251" Margin="0,3"/>
            </StackPanel>
        </GroupBox>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="OK" Click="Ok_Click" Width="70" Height="28" Margin="0,0,8,0" IsDefault="True"/>
            <Button Content="{DynamicResource Cancel}" Click="Cancel_Click" Width="70" Height="28" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>