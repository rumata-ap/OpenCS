<Window x:Class="OpenCS.Views.CsvSettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Экспорт в CSV" Width="360" Height="260"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <GroupBox Grid.Row="0" Header="Разделитель колонок" Margin="0,0,0,10">
            <StackPanel>
                <RadioButton x:Name="CsvSemicolon" Content="Точка с запятой ( ; )" Margin="0,3"/>
                <RadioButton x:Name="CsvComma" Content="Запятая ( , )" Margin="0,3"/>
            </StackPanel>
        </GroupBox>

        <GroupBox Grid.Row="1" Header="Кодировка">
            <StackPanel>
                <RadioButton x:Name="CsvUtf8" Content="UTF-8" Margin="0,3"/>
                <RadioButton x:Name="CsvWin1251" Content="Windows-1251" Margin="0,3"/>
            </StackPanel>
        </GroupBox>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="OK" Click="Ok_Click" Width="70" Height="28" Margin="0,0,8,0" IsDefault="True"/>
            <Button Content="Отмена" Click="Cancel_Click" Width="70" Height="28" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
