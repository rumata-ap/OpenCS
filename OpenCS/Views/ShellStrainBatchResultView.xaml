<UserControl x:Class="OpenCS.Views.ShellStrainBatchResultView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Шапка: вердикт -->
        <Border Grid.Row="0" Padding="8,6" Margin="0,0,0,8" CornerRadius="4"
                Background="{Binding StatusBrush}">
            <TextBlock Text="{Binding SummaryText}" FontWeight="Bold"
                       HorizontalAlignment="Center"/>
        </Border>

        <!-- Таблица строк -->
        <DataGrid Grid.Row="1" x:Name="RowsGrid" ItemsSource="{Binding Rows}" AutoGenerateColumns="False"
                  IsReadOnly="True" GridLinesVisibility="Horizontal"
                  CanUserSortColumns="True" HeadersVisibility="Column"
                  SelectionMode="Single" SelectionUnit="FullRow"
                  MouseDoubleClick="RowsGrid_MouseDoubleClick">
            <DataGrid.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="{DynamicResource CalcTaskNew}"
                              Click="CreateTask_Click"/>
                    <Separator/>
                    <MenuItem Header="{DynamicResource ExportCsvBtn}"
                              Click="ExportCsv_Click"/>
                </ContextMenu>
            </DataGrid.ContextMenu>
            <DataGrid.Columns>
                <DataGridTextColumn Header="{DynamicResource StrainStateBatch_ColNum}" Binding="{Binding Num}" Width="40"/>
                <DataGridTextColumn Header="{DynamicResource ShellStrainColLabel}" Binding="{Binding Label}" Width="*"/>
                <DataGridTextColumn Header="{DynamicResource ShellStrainColStatus}" Binding="{Binding Status}" Width="120"/>
                <DataGridTextColumn Header="{DynamicResource ShellStrainColVerdict}" Binding="{Binding Verdict}" Width="120"/>
                <DataGridTextColumn Header="{DynamicResource ShellStrainColFormula}" Binding="{Binding Formula}" Width="120"/>
                <DataGridTextColumn Header="{DynamicResource ShellStrainColIter}" Binding="{Binding Iterations}" Width="90"/>
                <DataGridTextColumn Header="{DynamicResource ShellStrainColResidual}" Binding="{Binding Residual}" Width="120"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Кнопки внизу -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,8,0,0">
            <Button Content="{DynamicResource CalcTaskNew}"
                    Click="CreateTask_Click"
                    Padding="12,4" Margin="0,0,8,0"
                    x:Name="CreateTaskBtn" IsEnabled="False"/>
            <Button Content="{DynamicResource ExportCsvBtn}"
                    Click="ExportCsv_Click"
                    Padding="12,4"/>
        </StackPanel>
    </Grid>
</UserControl>
